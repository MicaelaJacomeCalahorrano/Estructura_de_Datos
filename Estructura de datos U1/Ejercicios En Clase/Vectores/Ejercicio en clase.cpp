#include <iostream>
#include <vector>

using namespace std;

template <typename T>
class Persona
{
	private:
		string nombre;
		typedef long long int Cedula;
		unsigned int edad;
	public: 
		Persona (string nombre, int edad, Cedula cedula );
		
		int sumaEdades(int n)
		{
			if (i==0)
			{
				return Persona.edad[i];
			}else
			{
				for (int i=0; i<3 ; i++ )
				suma+= Persona.edad[i];
			}
		}
		
};
